package main

// ── main-package-level constants ────────────────────────────────────
// Shared constants live in internal/constants.go so every package can
// import them.  Keep this file for main-only values if needed.
